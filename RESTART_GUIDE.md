# 환경 변수 변경 후 앱 재시작 가이드

## 방법 1: 터미널에서 재시작

1. 현재 실행 중인 개발 서버를 **Ctrl + C**로 중지
2. 다음 명령어로 캐시를 클리어하고 재시작:

```bash
npx expo start -c
```

3. 스마트폰 앱도 **완전히 종료 후 재실행**

## 방법 2: 그래도 안 되면

네이버 개발자 센터에서 **Client Secret을 재발급** 받아야 할 수 있습니다.

1. https://developers.naver.com/apps/#/list 접속
2. 해당 애플리케이션 클릭
3. "Client Secret" 옆의 **"재발급"** 버튼 클릭
4. 새로 발급된 Secret을 `.env` 파일에 업데이트
5. 다시 `npx expo start -c`로 재시작

## 주의사항

- 환경 변수(`.env`) 변경은 **반드시 앱 재시작**이 필요합니다
- Hot reload로는 적용되지 않습니다
- 스마트폰 앱도 백그라운드에서 제거 후 완전히 재실행해야 합니다
